# Environment variables for local development
# Copy this file to .env and update values as needed
# DO NOT commit .env files with real values

# Backend API URL - use auth proxy for IAM-protected backends
BACKEND_BASE_URL=https://rg-dynamic-persona-auth-proxy-1036279278510.europe-west9.run.app

# Authentication mode: none, bearer, iap
AUTH_MODE=none

# Bearer token for direct Cloud Run IAM access (if not using proxy)
BEARER_TOKEN=

# IAP settings for local development (typically disabled)
IAP_MODE=true

# IAP audience (leave empty for local development)
IAP_AUDIENCE=

# Public path (usually root for local development)
APP_PUBLIC_PATH=/

USE_CORS_PROXY=false

# Example configurations:

# For local development with mock backend:
# BACKEND_BASE_URL=http://localhost:3000/api
# AUTH_MODE=none

# For Cloud Run backend via auth proxy:
# BACKEND_BASE_URL=https://your-auth-proxy-url
# AUTH_MODE=none

# For direct Cloud Run backend (requires manual token):
# BACKEND_BASE_URL=https://rg-dynamic-persona-1036279278510.europe-west9.run.app
# AUTH_MODE=bearer
# BEARER_TOKEN=your-identity-token

# Example usage:
# docker run -p 8080:8080 --env-file .env your-image-name
