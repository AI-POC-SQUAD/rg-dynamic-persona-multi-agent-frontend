# Environment variables for production deployment
# This file is copied to .env during Docker build

# Backend API URL - Auth Proxy that handles CORS and IAM authentication
BACKEND_BASE_URL=https://rg-dynamic-persona-auth-proxy-1036279278510.europe-west4.run.app

# Authentication mode: none (proxy handles auth)
AUTH_MODE=none

# Bearer token (not needed - proxy handles auth)
BEARER_TOKEN=

# IAP settings (disabled - using proxy)
IAP_MODE=false

# IAP audience (not needed)
IAP_AUDIENCE=

# Public path (root)
APP_PUBLIC_PATH=/

USE_CORS_PROXY=false

# Example configurations:

# For local development with mock backend:
# BACKEND_BASE_URL=http://localhost:3000/api
# AUTH_MODE=none

# For Cloud Run backend via auth proxy:
# BACKEND_BASE_URL=https://your-auth-proxy-url
# AUTH_MODE=none

# For direct Cloud Run backend (requires manual token):
# BACKEND_BASE_URL=https://rg-dynamic-persona-1036279278510.europe-west9.run.app
# AUTH_MODE=bearer
# BEARER_TOKEN=your-identity-token

# Example usage:
# docker run -p 8080:8080 --env-file .env your-image-name
